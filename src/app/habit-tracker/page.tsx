import HabitTracker from '@/Components/Habit Tracker/HabitTracker';
import React from 'react';

const page = () => {
    return (
        <div>
            <HabitTracker /> 
        </div>
    );
};

export default page;