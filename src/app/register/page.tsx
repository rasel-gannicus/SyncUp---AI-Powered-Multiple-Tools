import Register from '@/Components/Register & Login/Register/Register';
import React from 'react';

const page = () => {
    return (
        <div>
            <Register />
        </div>
    );
};

export default page;